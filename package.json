{
  "name": "intelligent-research-assistant",
  "version": "1.0.0",
  "description": "AI-powered research agent that autonomously researches topics and produces structured reports",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "build:all": "node scripts/deploy.js",
    "start": "ls -la server/ && ls -la server/dist/ && cd server && node dist/index.js",
    "start:prod": "cd server && NODE_ENV=production node dist/index.js",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}